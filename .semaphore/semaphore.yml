version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: build image
    task:
      jobs:
        - name: get code
          commands:
            - checkout
            - ./get-buildroot
        - name: build-config
          commands:
            - ./build-config 3
        - name: compile
          commands:
            - ./compile 3
